//import vec
import type2

data t0 {
    a: t1
    b: t2
    y: shared type2.d0
}

data t1 {
    c: s1
}

data t2 {
    d: u64
    e: bool
}

func mkt0() {
    let a = a(false) // Ref<t0>
    let b = b(10)    // Ref<t0>
    let c = t0(true, 10)
}

struct s1 {
    x : u32
    y : bool
    w : Either<u32, bool>
}

func mkt1() {
    let a = t1(10, true, left(20))
}

data Either<`a, `b> {
    Left : `a
    Right : `b
}

data Maybe<`a> {
    Just : `a
    Nothing
}

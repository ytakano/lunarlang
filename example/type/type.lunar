import vec

union t0 {
    a: bool
    b: u32
}

struct t1 {
    x : u32
    y : bool
    w : either<u32, bool>
}

union either<`a, `b> {
    left : `a
    right : `b
}

union maybe<`a> {
    just : `a
    nothing
}

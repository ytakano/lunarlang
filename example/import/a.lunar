import b
import b
import d here

class buz<`a> {
    h :: func(`a) -> `a
}

data st2<`a> {
    v: `a
}

instance b.c.foo<st> {
    func f(a) { a }
}

instance b.c.foo<st2> require buz<`a> {
    func g(a) { a }
}

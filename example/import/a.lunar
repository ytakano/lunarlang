import b
import d here

class buz<`a> {
    func h(`a) : `a
}

struct st2<`a> {
    v: `a
}

instance b.c.foo<st> {
    func f(a) { a }
}

instance b.c.foo<st2> require buz<`a> {
    func g(a) { a }
}
